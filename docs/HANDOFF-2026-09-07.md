# Colibri handoff — 2026-09-07

## Repository state

- Repository: `C:\Users\jaapj\.lmstudio\models\colibri-dev`
- Branch: `feature/m3-execution-step4-6`
- HEAD: `b663f54 docs(m3): close step6a gate`
- Remotes: `origin=https://github.com/jtinbergen/colibri.git`,
  `upstream=https://github.com/JustVugg/colibri.git`
- There are extensive uncommitted and untracked changes. Nothing from this
  session was committed or pushed. Preserve the existing worktree; do not use
  reset/checkout cleanup.

## Planner status

- Step 7 C/M is complete within the bounded shadow-only scope.
- Overall Gate 7 remains `NOT_GATED / NOT_PROMOTED`.
- Step 8a may begin as shadow-only preparation, per the dependency table.
- Active planner admission remains disabled. Synthetic profiles and unknown
  cache-condition captures must not drive scheduling.
- Transition record:
  `docs/results/m3-execution-step7-cm-transition-2026-09-07.md`
- Authoritative plan:
  `docs/minimax-m3-execution-plan-2026-09-05.md`

## Evidence added today

- Full-model M3 configuration/runtime smoke: 7,296 experts, 14,592 copy rows;
  453 completed observations covering 444 identities; shadow-off/on semantic
  engine traces equal at 10,366 events each.
- Full-model report:
  `docs/results/m3-execution-step7-full-model-runtime-report-2026-09-07.json`
- Three-drive capture:
  C: NVMe drive 101/controller 201, E: NVMe drive 102/controller 202, and
  D: Kingston XS1000 USB drive 103 with deliberately unknown controller.
  Singles, all pairs, and the triple were measured at 16 MiB and inflight
  1/2/4/8, eight samples per cell.
- Three-drive evidence:
  `docs/results/m3-execution-step7-calibration-3drive-2026-09-07.md`
- USB was materially slower: first read about 581 ms; inflight-8 p95 about
  3.024 s. Cache state is mixed/unknown, so this is not an admission bound.
- Machine profile bridge was exercised against the M3 directory; `coli plan`
  read it without applying calibration limits. Generated local profile:
  `c/.step7_machine_profile.json`.

## Verification

- Runtime replay: PASS.
- Python suite (trace report, calibration, profile, resource plan): 71 tests,
  PASS.
- Full-model runtime report validation: PASS.
- `git diff --check`: PASS.
- Popper/Sol scoped review of the full-model generator: no blockers.

## GitHub Actions status

- Local workflow:
  `.github/workflows/m3-step7-shadow.yml`
- It runs the Step-7 fixtures plus ASan/UBSan/TSan on Ubuntu.
- The workflow is still local/untracked and is not on remote `dev`; therefore
  `gh workflow run m3-step7-shadow.yml ...` returned HTTP 404.
- In the user's normal PowerShell, `gh api user --jq .login` returns
  `jtinbergen`. The Codex shell has a separate failing API/keyring/proxy path;
  do not treat its `gh auth status` as proof that the user's session is bad.
- To run Actions tomorrow, first decide what to commit/push and put the
  workflow on the remote branch. Then, from the user's working PowerShell:

  ```powershell
  cd C:\Users\jaapj\.lmstudio\models\colibri-dev
  gh workflow run m3-step7-shadow.yml --repo jtinbergen/colibri --ref dev
  gh run list --repo jtinbergen/colibri --workflow m3-step7-shadow.yml --limit 1
  ```

## Remaining Gate-7 work

1. Run hosted sanitizer workflow after the relevant changes are safely pushed.
2. Obtain cache-classified/held-out calibration and a pre-registered P
   criterion.
3. Measure true shared-controller/upstream contention if suitable hardware is
   available; do not infer it for the USB drive.
4. All-resident performance remains unavailable on this 32 GiB host for the
   roughly 224 GiB model.
5. Obtain the required full final Step-7 diff review.

Next safe engineering boundary: prepare 8a shadow-only fixtures/logging;
keep activation off and preserve no-profile fallback.
