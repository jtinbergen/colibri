# Packet: Slice 0c — QD/utilization truth on machine A

Deliverable: a single machine-A measurement report that decides whether the
parallel-issue thesis is alive. No code beyond an additive counter patch.

## 1. Scope and invariants

- Host: dev, i5-12600K, Windows 11, 32 GB. Two NVMe drives C: 980 PRO +
  E: PM9B1 on independent Standard NVM Express Controllers (per
  `docs/results/m3-execution-step7-calibration-2026-09-07.md`).
- Behavior unchanged. Opt-in via existing `PROF=1 DISK_SPLIT=1`.
- No scheduler, routing, kernel, or placement change.
- New counters are additive and read-only from `prof_report`.

## 2. Tasks

### 2.1 Inventory (read-only)

Inspect and report what already exists:

- `c/colibri.c:1002-1029` — `pipe_prof_*` inflight/overlap instrumentation.
- `c/colibri.c:1071-1094` — `dc_wall_*` busy-wall per class.
- `c/colibri.c:9703-9736` — `prof_report` DISK-CLASS lines (cold/warm counts,
  thread-seconds, busy-wall, aggregate GB/s-wall, avg-conc, disk-busy %).
- `c/colibri.c:9645-9750` — broader `prof_report` layout (read service,
  felt wait, time shares, expert-I/O share).
- Existing per-run flags: `PROF`, `DISK_SPLIT`, `DIRECT`, `PIPE`,
  `PIPE_WORKERS`, `RAM_GB`, `PIN_GB`, `AUTOPIN`, `COLI_RAM_OVERCOMMIT`,
  `COLI_DISKCLASS_WINDOW`.

Deliverable: a single bullet list of what is and is not already reported.
Specifically answer: is there a decode-window QD histogram, or only point
counters (`g_pipe_prof_io_inflight` sampled at transitions)?

### 2.2 Counter patch (only if gap)

If the inventory shows no decode-window QD histogram, propose a minimal
additive patch: a fixed-size bucket array of inflight counts sampled at the
same transitions as `pipe_prof_transition`, exposed in `prof_report` as
`QD histogram: …`. The patch must:

- Be one PR.
- Touch only `c/colibri.c` (no header changes other than `prof_base` layout).
- Not reorder, repurpose, or remove any existing counter.
- Not introduce nested locks; reuse the existing `g_pipe_prof_mx`.

Concurrency diff review is required before merge (per AGENTS routing for
concurrency / memory-ordering changes).

### 2.3 Prescribed runs

For each configuration below, run 5x with alternating order. Document
warm-up and cache state explicitly. Use a fixed prompt and 32 decode tokens.

| Config | Command sketch |
|---|---|
| old baseline | C: only, `PIPE=0`, `RAM_GB=18`, `AUTOPIN=0`, `COLI_RAM_OVERCOMMIT=1` |
| PIPE=1 old | C: only, `PIPE=1 PIPE_WORKERS=4`, same RAM/autopin |
| Step 4 | `COLI_M3_DAG_SERIAL=1 COLI_M3_DAG_PIPE=1` on top of PIPE=1 |
| Step 6 | `COLI_M3_DAG_SERIAL=1 COLI_M3_DAG_PIPE=1 COLI_M3_DAG_PARALLEL=1 COLI_M3_DAG_WORKERS=4` |

Add `PROF=1 DISK_SPLIT=1` to every run. Save raw `prof_report` blocks per run.

### 2.4 Report

Artifact: `docs/results/m3-execution-step0c-qd-<date>.md`. Required content:

- Literal commands, build checksum, effective config per run.
- Per-run: QD histogram (mean / p50 inflight), disk-busy %, I/O-active,
  compute-active, concurrent, felt wait, tok/s, RSS, bytes, decode wall.
- 5-run summary table with median + min/max.
- Cache condition labelled per run (`warm after prefill`).
- Verdict: `QD~1 + disk-busy ~25%` / `QD already high` / `INCONCLUSIVE`.

## 3. Gate 0c

- C: PASS if `prof_report` outputs are byte-identical with and without the
  new counter.
- M: PASS if at least one run’s histogram explains an observed wait in the
  existing timers.
- P: no perf claim. Verdict from §2.4 drives the A-track decision.

Stop conditions:

- QD already high → stop A1/A2, report thesis dead, do not build executor
  work to chase a stalled mechanism.
- INCONCLUSIVE → re-run with documented change, no selective A/B.
